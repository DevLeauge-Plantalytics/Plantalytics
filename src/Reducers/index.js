/*jshint esversion: 6*/
// import {} from '../Actions';
const initialState = {
  plants: []
};
const plants = (state = initialState, action) => {
  switch(action.type){
    default: {
      return state;
    }
  }
};

export default plants;